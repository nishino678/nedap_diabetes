<% if @fact.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      Dit formulier bevat <%= @fact.errors.count %> fout(en).
    </div>
    <% @fact.errors.full_messages.each do |msg| %>
      <%= msg.capitalize %><br />
    <% end %>
  </div>
<% end %>
