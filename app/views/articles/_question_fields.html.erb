<b>Vraag bij artikel</b><br />
  <%= f.text_area :content, :rows => 3, class:'textarea', placeholder:"Vraag.." %>

  <%= f.fields_for :answers do |builder| %>
  <%= render 'answer_fields', :f => builder %>

  <% end %>