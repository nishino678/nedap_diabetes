<% @back_or_search = "back" %>
<%= render 'shared/header' %>

<div class="specialisten admin-buttons">
  <%= link_to 'Specialist toevoegen', new_specialist_path, class:"button blue" %>
</div>

<div class="specialist-group">
  <% @specialist.each do |specialist| %>
    <ul class="specialist">
      <li><%= link_to specialist.name, specialist %></li>
      <li class="admin-buttons"><%= link_to 'bewerk specialist', edit_specialist_path(specialist), class:"button blue" %></li>
      <li class="admin-buttons"><%= link_to 'verwijder specialist', specialist, method: :delete, data: { confirm: "Weet je het zeker?" }, class:"button white"%></li>
    </ul>
  <% end  %>
</div>

<%= render 'shared/tabbar' %>
